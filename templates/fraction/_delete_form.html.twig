<form method="post" action="{{ path('app_fraction_delete', {'id': fraction.id}) }}" onsubmit="return confirm('Czy na pewno chcesz usunąć tę frakcję?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fraction.id) }}">
    <button class="btn">Delete</button>
</form>
