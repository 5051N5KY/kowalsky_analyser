<form method="post" action="{{ path('app_shperacze_delete', {'id': shperacze.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ shperacze.id) }}">
    <button class="btn">Delete</button>
</form>
